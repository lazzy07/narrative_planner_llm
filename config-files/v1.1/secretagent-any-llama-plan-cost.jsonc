{
  "search": {
    // Search configurations
    "type": {
      "name": "best-first", // Options: breadth-first
      "config": {},
    },
    "cost": {
      "type": "plan-cost", // Options: action-cost / plan-cost / none
      "config": {},
    },
    "plan": {
      "max-length": 9, // Max length of the plan untill the planner should stop, if planner did not find a solution.
      "utility": 1, // Utility that planner needs to achieve.
      "max-nodes": 5000,
    },
  },
  "llm": {
    // LLM API Configurations
    "prompt": {
      "version": "v1.2",
      "directory": "prompt-templates/",
      "explanation": true, // Append the explaination to (coming from the LLM) why that action was taken.
    },
    "cache": {
      "enabled": true,
      "directory": ".llama-8b-cache/",
    },
    "model": {
      "name": "llama-8b",
      "config": {},
    },
  },
  "domain": {
    // Domain configurations
    "name": "secretagent",
    "file": "problems/secretagent.txt",
  },
  "output": {
    "save": true,
    "directory": "output/",
  },
}
